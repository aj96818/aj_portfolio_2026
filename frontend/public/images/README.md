Image folders for the site.

- `home/background.jpg`: homepage hero background image.
- `portfolio/photo-1.jpg` through `portfolio/photo-12.jpg`: photography grid images.

Use `.jpg` files for the current defaults. The filenames are intentionally fixed so the app can pick them up without another code change.
